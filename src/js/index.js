import "../sass/style.scss"
window.onload = function () {
  // If JavaScript disabled, allow .no-js styles
  const html = document.querySelector("html")
  html.classList.remove("no-js")
}
import "./test"
